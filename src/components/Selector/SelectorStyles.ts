import styled from 'styled-components';

export const Title = styled.label`
  font-weight: 700;
  padding: 0.5rem 0;
`;

export const TitleWithActions = styled.div`
  display: flex;
  gap: 0.5rem;
  align-items: center;
`;

export const ActionButton = styled.button`
  background: none;
  border: none;
  display: flex;
`;
